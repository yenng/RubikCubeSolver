#ifndef solver_H
#define solver_H
#include "rotation.h"
#include "cube.h"

void sideCornerSolver1(Cube *cube);
void fullSideCornerSolver(Cube *cube);
void sideCornerSolver(Cube *cube);
#endif // solver_H
